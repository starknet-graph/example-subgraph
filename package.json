{
  "name": "example-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined starknet/example",
    "create-local": "graph create --node http://localhost:8020/ starknet/example",
    "remove-local": "graph remove --node http://localhost:8020/ starknet/example",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 starknet/example",
    "test": "graph test"
  },
  "dependencies": {
    "@starknet-graph/graph-cli": "0.58.0-1",
    "@starknet-graph/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
